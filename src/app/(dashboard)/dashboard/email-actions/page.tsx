import EmailActionComponent from "@/app/pageComponents/Dashboard/EmailActionComponent";

const EmailActions = async () => {
  return (
    <div className="graphSection">
      <EmailActionComponent />
    </div>
  );
};

export default EmailActions;
